const components = {
  MuiButton: {
    styleOverrides: {
      root: {
        boxShadow: "initial",
        "&:hover": {
          boxShadow: "initial",
        },
      },
    },
  },
};

export default components;
